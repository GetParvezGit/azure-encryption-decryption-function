# azure-encryption-decryption-function
This project demonstrates a secure PGP-based encryption and decryption solution built with Azure Functions and C#. It accepts input through HTTP request bodies, encrypts the data using a PGP public key, and decrypts encrypted payloads using the corresponding private key and passphrase.
